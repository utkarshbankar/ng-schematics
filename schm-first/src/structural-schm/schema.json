{
  "$schema": "http://json-schema.org/schema",
  "$id": "structuralSchematic",
  "title": "ng new options schema",
  "type": "object",
  "description": "Update the file and project structure",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to add module federation",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Project name (press enter for default project)"
    }
  }
}